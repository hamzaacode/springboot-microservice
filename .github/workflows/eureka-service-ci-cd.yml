name: eureka-service

on:
  push:
    branches: [ main, kubernates-deployment-fix-2 ]
    paths:
      - 'eureka-server/**'
      - '.github/workflows/eureka-service-ci-cd.yml'
      - 'shared/**'
  workflow_dispatch:

env:
  SERVICE_DIR: eureka-server
  IMAGE_TAG: eureka-service:latest
  DEPLOY_DIR: eureka-server/k8s

concurrency:
  group: eureka-deploy
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Ensure Minikube is running (robust)
        shell: bash
        run: |
          set -euxo pipefail
          verify_minikube() {
            minikube status >/dev/null 2>&1 && \
            kubectl config use-context minikube >/dev/null 2>&1 && \
            kubectl cluster-info >/dev/null 2>&1
          }
          echo "🔍 Checking Minikube status..."
          if verify_minikube; then
            echo "✅ Minikube is running and accessible"
          else
            echo "❌ Minikube is not accessible, attempting recovery..."
            minikube stop || true
            sleep 5
            echo "🚀 Starting Minikube..."
            minikube start \
              --driver=docker \
              --memory=4096 \
              --cpus=2 \
              --disk-size=20g \
              --wait=true \
              --timeout=10m
            sleep 15
            if verify_minikube; then
              echo "✅ Minikube recovery successful"
            else
              echo "❌ Minikube recovery failed"
              minikube logs || true
              exit 1
            fi
          fi
          kubectl config use-context minikube
          kubectl get nodes
          echo "🎉 Minikube is ready!"

      - name: Configure Docker environment
        shell: bash
        run: |
          set -euxo pipefail
          eval $(minikube docker-env)
          echo "DOCKER_HOST=$DOCKER_HOST" >> $GITHUB_ENV
          echo "DOCKER_TLS_VERIFY=$DOCKER_TLS_VERIFY" >> $GITHUB_ENV
          echo "DOCKER_CERT_PATH=$DOCKER_CERT_PATH" >> $GITHUB_ENV

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Use persistent Maven repository
        shell: bash
        run: |
          set -euxo pipefail
          mkdir -p /opt/maven-repo
          rm -rf ~/.m2/repository
          mkdir -p ~/.m2
          ln -s /opt/maven-repo ~/.m2/repository
          echo "✅ Using persistent Maven repo at /opt/maven-repo"

      - name: Build fat JAR (optimized)
        shell: bash
        run: |
          set -euxo pipefail
          cd "$SERVICE_DIR"
          mvn clean package -DskipTests -T 1C --batch-mode --no-transfer-progress \
            -Dmaven.javadoc.skip=true -Dmaven.source.skip=true

      - name: Build Docker image
        shell: bash
        env:
          DOCKER_BUILDKIT: 1
        run: |
          set -euxo pipefail
          docker build \
            --cache-from "$IMAGE_TAG" \
            --build-arg BUILDKIT_INLINE_CACHE=1 \
            -t "$IMAGE_TAG" \
            "$SERVICE_DIR"

      - name: Deploy to Kubernetes
        shell: bash
        run: |
          set -euxo pipefail
          kubectl apply -f "$DEPLOY_DIR"
          if kubectl get deployment eureka-deployment >/dev/null 2>&1; then
            kubectl rollout restart deployment/eureka-deployment
          fi
          kubectl rollout status deployment/eureka-deployment --timeout=120s

      - name: Quick health check
        shell: bash
        run: |
          set -euxo pipefail
          kubectl wait --for=condition=ready pod -l app=eureka-service --timeout=180s
          kubectl port-forward svc/eureka-service 8761:8761 &
          PF_PID=$!
          sleep 5
          curl --fail http://localhost:8761/actuator/health
          kill $PF_PID

      - name: Post-deploy info (on failure only)
        if: failure()
        shell: bash
        run: |
          echo "=== PODS ==="
          kubectl get pods -o wide
          echo "=== SERVICES ==="
          kubectl get svc -o wide
          echo "=== RECENT EVENTS ==="
          kubectl get events --sort-by=.lastTimestamp | tail -n 10
          echo "=== EUREKA LOGS ==="
          kubectl logs -l app=eureka-service --tail=30
