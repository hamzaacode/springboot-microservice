name: Deploy Order to Minikube

on:
  push:
    branches: [ main, kubernates-deployment-fix ]
    paths:
      - 'order-server/**'
      - '.github/workflows/order-service-ci-cd.yml'
      - 'shared/**'
  workflow_dispatch:

env:
  SERVICE_DIR: order-server
  IMAGE_TAG: order-service:latest
  DEPLOY_DIR: order-server/k8s

concurrency:
  group: order-deploy
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Ensure Minikube is running (robust)
        shell: bash
        run: |
          set -euxo pipefail
          
          # Function to verify Minikube is truly working
          verify_minikube() {
            minikube status >/dev/null 2>&1 && \
            kubectl config use-context minikube >/dev/null 2>&1 && \
            kubectl cluster-info >/dev/null 2>&1
          }
          
          echo "🔍 Checking Minikube status..."
          
          if verify_minikube; then
            echo "✅ Minikube is running and accessible"
          else
            echo "❌ Minikube is not accessible, attempting recovery..."
          
            # Try to stop any hanging processes
            minikube stop || true
            sleep 5
          
            # Start Minikube with proper settings
            echo "🚀 Starting Minikube..."
            minikube start \
              --driver=docker \
              --memory=4096 \
              --cpus=2 \
              --disk-size=20g \
              --wait=true \
              --timeout=10m
          
            # Wait a bit for full initialization
            sleep 15
          
            # Verify it's working
            if verify_minikube; then
              echo "✅ Minikube recovery successful"
            else
              echo "❌ Minikube recovery failed"
              minikube logs || true
              exit 1
            fi
          fi
          
          # Set context and verify kubectl works
          kubectl config use-context minikube
          kubectl get nodes
          echo "🎉 Minikube is ready!"

      - name: Configure Docker environment
        shell: bash
        run: |
          set -euxo pipefail
          eval $(minikube docker-env)
          echo "DOCKER_HOST=$DOCKER_HOST" >> $GITHUB_ENV
          echo "DOCKER_TLS_VERIFY=$DOCKER_TLS_VERIFY" >> $GITHUB_ENV
          echo "DOCKER_CERT_PATH=$DOCKER_CERT_PATH" >> $GITHUB_ENV

      - name: Setup Java (cached)
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Cache Maven dependencies (improved)
        uses: actions/cache@v4
        with:
          path: |
            ~/.m2/repository
            ~/.m2/wrapper
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml', '**/maven-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Build fat JAR (optimized)
        shell: bash
        run: |
          set -euxo pipefail
          cd "$SERVICE_DIR"
          mvn clean package -DskipTests -T 1C --batch-mode --no-transfer-progress \
            -Dmaven.javadoc.skip=true -Dmaven.source.skip=true

      - name: Build Docker image (with cache)
        shell: bash
        env:
          DOCKER_BUILDKIT: 1
        run: |
          set -euxo pipefail
          docker build \
            --cache-from "$IMAGE_TAG" \
            --build-arg BUILDKIT_INLINE_CACHE=1 \
            -t "$IMAGE_TAG" \
            "$SERVICE_DIR"

      - name: Deploy to Kubernetes (optimized)
        shell: bash
        run: |
          set -euxo pipefail
          kubectl apply -f "$DEPLOY_DIR"
          if kubectl get deployment order-deployment >/dev/null 2>&1; then
            kubectl rollout restart deployment/order-deployment
          fi
          kubectl rollout status deployment/order-deployment --timeout=120s

      - name: Quick health check
        shell: bash
        run: |
          set -euxo pipefail
          # Wait for pods to be ready
          kubectl wait --for=condition=ready pod -l app=eureka-service --timeout=180s
          # Forward the service to localhost in the background
          kubectl port-forward svc/eureka-service 8081:8081 &
          PF_PID=$!
          # Give it a moment to establish
          sleep 5
          # Test the health endpoint
          curl --fail http://localhost:8081/actuator/health
          # Kill port-forward process
          kill $PF_PID

      - name: Post-deploy info (on failure only)
        if: failure()
        shell: bash
        run: |
          echo "=== PODS ==="
          kubectl get pods -o wide
          echo "=== SERVICES ==="
          kubectl get svc -o wide
          echo "=== RECENT EVENTS ==="
          kubectl get events --sort-by=.lastTimestamp | tail -n 10
          echo "=== order LOGS ==="
          kubectl logs -l app=order-service --tail=30
